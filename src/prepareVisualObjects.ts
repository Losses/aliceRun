import * as THREE from "three";

import { ResourceTracker } from "./ResourceTracker";

export const prepareVisualObjects = (
  scene: THREE.Scene,
  tracker: ResourceTracker
) => {
  return {
  };
};
